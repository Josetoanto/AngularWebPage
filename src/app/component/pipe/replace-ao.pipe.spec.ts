import { ReplaceAOPipe } from './replace-ao.pipe';

describe('ReplaceAOPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceAOPipe();
    expect(pipe).toBeTruthy();
  });
});
